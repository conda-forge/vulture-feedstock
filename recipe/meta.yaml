{% set name = "vulture" %}
{% set version = "2.5" %}
{% set sha256 = "2831694055eb2e36a09c3b7680934837102b9b6c0969206e3902d513612177c3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  entry_points:
    - vulture = vulture.core:main

requirements:
  host:
    - python >=3.6
    - pip
  run:
    - python >=3.6
    - toml

test:
  requires:
    - pip
  imports:
    - vulture
  commands:
    - pip check
    - vulture --help

about:
  home: https://github.com/jendrikseipp/vulture
  license: MIT
  license_file: LICENSE.txt
  summary: Find dead code

extra:
  recipe-maintainers:
    - jakirkham
